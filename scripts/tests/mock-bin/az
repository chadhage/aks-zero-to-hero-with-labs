#!/usr/bin/env bash

set -euo pipefail

if [[ "$*" == *'acr show'* ]]; then
  printf 'mock.azurecr.io\n'
elif [[ "$*" == *'acr manifest list-metadata'* ]]; then
  printf 'sha256:%064d\n' 1
else
  printf 'Unexpected mocked az invocation: %s\n' "$*" >&2
  exit 1
fi